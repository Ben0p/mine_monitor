# Re-build image
docker-compose up --build

# List of containers
docker ps

# Copy file to container
docker cp . b234aea68a0f:/app/src

# Not docker but to set system time from hardware clock:
hwclock --hctosys